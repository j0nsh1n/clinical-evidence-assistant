fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
biopython>=1.83
httpx>=0.27
pypdf>=6.0             # PDF drop-in: local text extraction, nothing uploaded
python-multipart>=0.0.9  # FastAPI file-upload support for /analyze-pdf
anthropic>=0.49        # optional — only needed for the LLM 'Refine with AI' feature

# Testing
pytest>=8.0
